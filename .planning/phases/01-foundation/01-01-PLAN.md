---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Site_Limpa_Fossa/package.json
  - Site_Limpa_Fossa/vite.config.ts
  - Site_Limpa_Fossa/tsconfig.json
  - Site_Limpa_Fossa/tsconfig.app.json
  - Site_Limpa_Fossa/tsconfig.node.json
  - Site_Limpa_Fossa/tailwind.config.js
  - Site_Limpa_Fossa/postcss.config.js
  - Site_Limpa_Fossa/src/index.css
  - Site_Limpa_Fossa/src/main.tsx
  - Site_Limpa_Fossa/src/App.tsx
  - Site_Limpa_Fossa/components.json
  - Site_Limpa_Fossa/src/lib/utils.ts
  - Site_Limpa_Fossa/src/components/ui/button.tsx
  - Site_Limpa_Fossa/src/components/ui/card.tsx
  - Site_Limpa_Fossa/src/components/ui/badge.tsx
  - Site_Limpa_Fossa/.gitignore
autonomous: true
requirements:
  - HERO-03
must_haves:
  truths:
    - "npm run dev starts Vite dev server on localhost without errors"
    - "Tailwind utility classes render correctly in the browser"
    - "shadcn/ui Button, Card, Badge components are importable and render"
    - "Brand colors (brand-green, brand-blue) are available as Tailwind classes"
    - ".gitignore contains .env* patterns before any commit"
  artifacts:
    - path: "Site_Limpa_Fossa/package.json"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "tailwindcss"
    - path: "Site_Limpa_Fossa/tailwind.config.js"
      provides: "Tailwind 3 config with brand colors"
      contains: "brand-green"
    - path: "Site_Limpa_Fossa/src/index.css"
      provides: "Tailwind directives and global styles"
      contains: "@tailwind base"
    - path: "Site_Limpa_Fossa/src/components/ui/button.tsx"
      provides: "shadcn/ui Button component"
      exports: ["Button"]
    - path: "Site_Limpa_Fossa/src/components/ui/card.tsx"
      provides: "shadcn/ui Card component"
      exports: ["Card", "CardHeader", "CardContent"]
    - path: "Site_Limpa_Fossa/src/components/ui/badge.tsx"
      provides: "shadcn/ui Badge component"
      exports: ["Badge"]
  key_links:
    - from: "Site_Limpa_Fossa/src/main.tsx"
      to: "Site_Limpa_Fossa/src/App.tsx"
      via: "React root render"
      pattern: "createRoot.*App"
    - from: "Site_Limpa_Fossa/src/index.css"
      to: "Site_Limpa_Fossa/tailwind.config.js"
      via: "Tailwind directives processed by PostCSS"
      pattern: "@tailwind base"
---

<objective>
Scaffold the Limpa Fossa Express project with Vite + React 18 + TypeScript + Tailwind CSS 3 + shadcn/ui, establishing the design token system and component foundation that all subsequent plans build on.

Purpose: A working, verified scaffold ensures every future plan builds on a stable foundation. Getting Tailwind 3 (not v4) and shadcn/ui right here prevents cascading breakage.
Output: A bootable Vite dev server in `Site_Limpa_Fossa/` with Tailwind 3, shadcn/ui components (Button, Card, Badge), brand colors, and system font stack configured.
</objective>

<execution_context>
@C:/Users/Johau/PROJETOS/Limpa_fossa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Johau/PROJETOS/Limpa_fossa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React 18 + TS and configure Tailwind 3 with design tokens</name>
  <files>
    Site_Limpa_Fossa/package.json,
    Site_Limpa_Fossa/vite.config.ts,
    Site_Limpa_Fossa/tsconfig.json,
    Site_Limpa_Fossa/tsconfig.app.json,
    Site_Limpa_Fossa/tsconfig.node.json,
    Site_Limpa_Fossa/tailwind.config.js,
    Site_Limpa_Fossa/postcss.config.js,
    Site_Limpa_Fossa/src/index.css,
    Site_Limpa_Fossa/src/main.tsx,
    Site_Limpa_Fossa/src/App.tsx,
    Site_Limpa_Fossa/.gitignore
  </files>
  <action>
    IMPORTANT: All commands run from inside `Site_Limpa_Fossa/` directory.

    1. Delete the existing `Site_Limpa_Fossa/README.md` placeholder. Then from the PARENT directory (`c:/Users/Johau/PROJETOS/Limpa_fossa/`), scaffold into the existing directory:
       ```bash
       cd Site_Limpa_Fossa
       npm init -y
       npm install react@18 react-dom@18
       npm install -D typescript @types/react @types/react-dom @vitejs/plugin-react vite
       ```
       Then manually create `vite.config.ts`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `src/main.tsx`, `src/App.tsx` using standard Vite React-TS template patterns.

       Alternatively, scaffold to a temp directory and move files:
       ```bash
       cd c:/Users/Johau/PROJETOS/Limpa_fossa
       npm create vite@latest temp-scaffold -- --template react-ts
       ```
       Then copy all files from `temp-scaffold/` into `Site_Limpa_Fossa/`, overwriting the README. Delete `temp-scaffold/`.

    2. Install Tailwind CSS 3 (NOT v4 -- v4 breaks shadcn/ui entirely):
       ```bash
       npm install -D tailwindcss@3 postcss autoprefixer
       npx tailwindcss init -p
       ```
       Verify after install: `npx tailwindcss --help` should show version 3.x.

    3. Configure `tailwind.config.js` with brand design tokens:
       ```js
       /** @type {import('tailwindcss').Config} */
       export default {
         content: [
           "./index.html",
           "./src/**/*.{js,ts,jsx,tsx}",
         ],
         theme: {
           extend: {
             colors: {
               'brand-green': '#16a34a',
               'brand-blue': '#2563eb',
             },
             fontFamily: {
               sans: [
                 'Inter', 'system-ui', '-apple-system', 'BlinkMacSystemFont',
                 'Segoe UI', 'Roboto', 'Helvetica Neue', 'Arial', 'sans-serif'
               ],
             },
           },
         },
         plugins: [],
       }
       ```

    4. Update `src/index.css` to include Tailwind directives and smooth scroll (for HERO-02 smooth scroll behavior):
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;

       html {
         scroll-behavior: smooth;
       }
       ```
       Remove ALL default Vite CSS (the purple/gradient demo styles).

    5. Clean up `src/App.tsx` to a minimal shell:
       ```tsx
       function App() {
         return (
           <div className="min-h-screen bg-white text-gray-900 font-sans">
             <h1 className="text-brand-green text-4xl font-bold p-8">
               Limpa Fossa Express
             </h1>
             <p className="text-brand-blue p-8">Scaffold OK</p>
           </div>
         )
       }
       export default App
       ```
       This verifies Tailwind + brand colors work visually.

    6. Update `.gitignore` to include BEFORE any commit (Pitfall 3 from PITFALLS.md):
       ```
       node_modules
       dist
       .env
       .env.local
       .env.production
       .env.*.local
       *.log
       ```

    7. Delete `src/App.css` and any Vite demo assets (svg logos, etc.) that ship with the template.

    8. Run `npm run dev` and verify no errors in terminal. Stop the server after verification.
  </action>
  <verify>
    <automated>cd Site_Limpa_Fossa && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run dev` starts without errors
    - `npm run build` produces `dist/` without errors
    - `tailwind.config.js` exists with `brand-green` and `brand-blue` in theme.extend.colors
    - `.gitignore` contains `.env` patterns
    - No Vite demo content remains (no default logos, no purple styles)
    - Tailwind version is 3.x (not 4.x)
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and install Button, Card, Badge components</name>
  <files>
    Site_Limpa_Fossa/components.json,
    Site_Limpa_Fossa/src/lib/utils.ts,
    Site_Limpa_Fossa/src/components/ui/button.tsx,
    Site_Limpa_Fossa/src/components/ui/card.tsx,
    Site_Limpa_Fossa/src/components/ui/badge.tsx
  </files>
  <action>
    All commands run from `Site_Limpa_Fossa/` directory.

    1. Install shadcn/ui dependencies first (clsx + tailwind-merge are required by shadcn's `cn()` utility):
       ```bash
       npm install clsx tailwind-merge class-variance-authority
       ```

    2. Create the `cn()` utility manually at `src/lib/utils.ts`:
       ```ts
       import { type ClassValue, clsx } from "clsx"
       import { twMerge } from "tailwind-merge"

       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs))
       }
       ```

    3. Initialize shadcn/ui:
       ```bash
       npx shadcn@latest init
       ```
       When prompted, choose:
       - Style: Default
       - Base color: Slate
       - CSS variables: Yes
       - tailwind.config.js location: tailwind.config.js
       - Components alias: @/components
       - Utils alias: @/lib/utils

       If the CLI asks about `tsconfig.json` paths, configure `@/*` to map to `./src/*` in both `tsconfig.json` and `tsconfig.app.json`:
       ```json
       {
         "compilerOptions": {
           "baseUrl": ".",
           "paths": {
             "@/*": ["./src/*"]
           }
         }
       }
       ```
       Also ensure `vite.config.ts` has the matching alias:
       ```ts
       import path from "path"
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'

       export default defineConfig({
         plugins: [react()],
         resolve: {
           alias: {
             "@": path.resolve(__dirname, "./src"),
           },
         },
       })
       ```

    4. Install the 3 required shadcn/ui components:
       ```bash
       npx shadcn@latest add button card badge
       ```
       This generates files in `src/components/ui/`.

    5. Install `lucide-react` (icon library used by shadcn/ui and needed for service cards in Plan 02):
       ```bash
       npm install lucide-react
       ```

    6. Verify components work by updating `src/App.tsx` to import and render them:
       ```tsx
       import { Button } from "@/components/ui/button"
       import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
       import { Badge } from "@/components/ui/badge"

       function App() {
         return (
           <div className="min-h-screen bg-white text-gray-900 font-sans p-8 space-y-4">
             <h1 className="text-brand-green text-4xl font-bold">
               Limpa Fossa Express
             </h1>
             <Badge className="bg-brand-green">Scaffold OK</Badge>
             <Card className="max-w-sm">
               <CardHeader>
                 <CardTitle>Test Card</CardTitle>
               </CardHeader>
               <CardContent>
                 <Button className="bg-brand-blue hover:bg-brand-blue/90">
                   Test Button
                 </Button>
               </CardContent>
             </Card>
           </div>
         )
       }
       export default App
       ```

    7. Run `npm run build` to confirm no TypeScript errors and no import resolution issues.
  </action>
  <verify>
    <automated>cd Site_Limpa_Fossa && npx tsc --noEmit 2>&1 | tail -10 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `components.json` exists at project root configuring shadcn/ui
    - `src/lib/utils.ts` exports the `cn()` function
    - `src/components/ui/button.tsx` exports Button
    - `src/components/ui/card.tsx` exports Card, CardHeader, CardContent, CardTitle, CardDescription, CardFooter
    - `src/components/ui/badge.tsx` exports Badge
    - `lucide-react` is installed in package.json
    - `@/*` path alias resolves correctly (imports from `@/components/ui/button` work)
    - `npm run build` succeeds with zero errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .gitignore -> git | Env files must never enter version control |
| npm registry -> project | Third-party packages installed from npm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env files | mitigate | .gitignore includes `.env*` patterns before first commit (Pitfall 3) |
| T-01-02 | Tampering | npm dependencies | accept | Standard npm install from public registry; lock file committed; low risk for static landing page |
</threat_model>

<verification>
1. From `Site_Limpa_Fossa/`, run `npm run dev` -- page loads at localhost showing brand-green heading, Badge, Card, and Button
2. Run `npm run build` -- produces `dist/` with zero errors
3. Confirm `npx tailwindcss --help` shows version 3.x
4. Confirm `.gitignore` contains `.env` entries
5. Confirm `@/` import alias works (build would fail if not)
</verification>

<success_criteria>
- Vite dev server boots without errors
- Build produces dist/ without errors
- Tailwind 3.4.x installed (NOT v4)
- Brand colors (brand-green #16a34a, brand-blue #2563eb) render correctly
- shadcn/ui Button, Card, Badge importable via @/ alias
- lucide-react installed for Plan 02 icon needs
- .gitignore protects env files
- No Vite demo content remains
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
