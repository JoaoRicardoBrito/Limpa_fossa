---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Site_Limpa_Fossa/src/App.tsx
  - Site_Limpa_Fossa/src/components/sections/Header.tsx
  - Site_Limpa_Fossa/src/components/sections/HeroSection.tsx
  - Site_Limpa_Fossa/src/components/sections/ServicesSection.tsx
  - Site_Limpa_Fossa/src/components/sections/Footer.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - HERO-01
  - HERO-02
  - HERO-03
  - SERV-01
  - CTA-03
must_haves:
  truths:
    - "Visitor sees header with 'Limpa Fossa Express' logo text and nav links at 360px without horizontal overflow"
    - "Visitor sees headline 'Limpeza de Fossa Rapida e Sem Complicacao' and CTA above the fold on 360px mobile"
    - "Clicking 'Agendar Agora' smooth-scrolls to the #agendamento anchor"
    - "Visitor sees 3 service cards (Limpeza de Fossa, Desentupimento, Caixa de Gordura) with icons and descriptions"
    - "Footer displays phone, WhatsApp, service area, and social links (Instagram, Facebook)"
    - "Page achieves LCP < 2.5s on simulated 4G mobile Lighthouse"
  artifacts:
    - path: "Site_Limpa_Fossa/src/components/sections/Header.tsx"
      provides: "Sticky header with logo text and mobile hamburger nav"
      exports: ["Header"]
    - path: "Site_Limpa_Fossa/src/components/sections/HeroSection.tsx"
      provides: "Hero with headline, subtitle, CTA button, service area badge"
      exports: ["HeroSection"]
    - path: "Site_Limpa_Fossa/src/components/sections/ServicesSection.tsx"
      provides: "3-card services grid responsive layout"
      exports: ["ServicesSection"]
    - path: "Site_Limpa_Fossa/src/components/sections/Footer.tsx"
      provides: "Footer with contact info and social links"
      exports: ["Footer"]
    - path: "Site_Limpa_Fossa/src/App.tsx"
      provides: "Root layout assembling all sections"
      exports: ["default App"]
  key_links:
    - from: "Site_Limpa_Fossa/src/App.tsx"
      to: "Header, HeroSection, ServicesSection, Footer"
      via: "import and render in sequence"
      pattern: "import.*from.*sections/"
    - from: "Site_Limpa_Fossa/src/components/sections/HeroSection.tsx"
      to: "#agendamento anchor"
      via: "href='#agendamento' on CTA button"
      pattern: "href.*#agendamento"
    - from: "Site_Limpa_Fossa/src/components/sections/ServicesSection.tsx"
      to: "shadcn/ui Card component"
      via: "import Card from @/components/ui/card"
      pattern: "import.*Card.*from.*@/components/ui/card"
---

<objective>
Build all four static UI sections (Header, Hero, Services, Footer) that compose the Phase 1 landing page, delivering a production-ready visual experience at 360px mobile and desktop breakpoints.

Purpose: This plan delivers the visible product -- the page visitors actually see. All 7 Phase 1 requirements (NAV-01, NAV-02, HERO-01, HERO-02, HERO-03, SERV-01, CTA-03) are implemented here.
Output: Complete landing page with Header, HeroSection, ServicesSection, and Footer rendered in App.tsx.
</objective>

<execution_context>
@C:/Users/Johau/PROJETOS/Limpa_fossa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Johau/PROJETOS/Limpa_fossa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 scaffold -- these are available for import -->

From Site_Limpa_Fossa/src/components/ui/button.tsx:
```typescript
export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: "default" | "destructive" | "outline" | "secondary" | "ghost" | "link"
  size?: "default" | "sm" | "lg" | "icon"
  asChild?: boolean
}
export const Button: React.FC<ButtonProps>
```

From Site_Limpa_Fossa/src/components/ui/card.tsx:
```typescript
export const Card: React.FC<React.HTMLAttributes<HTMLDivElement>>
export const CardHeader: React.FC<React.HTMLAttributes<HTMLDivElement>>
export const CardTitle: React.FC<React.HTMLAttributes<HTMLHeadingElement>>
export const CardDescription: React.FC<React.HTMLAttributes<HTMLParagraphElement>>
export const CardContent: React.FC<React.HTMLAttributes<HTMLDivElement>>
export const CardFooter: React.FC<React.HTMLAttributes<HTMLDivElement>>
```

From Site_Limpa_Fossa/src/components/ui/badge.tsx:
```typescript
export interface BadgeProps extends React.HTMLAttributes<HTMLDivElement> {
  variant?: "default" | "secondary" | "destructive" | "outline"
}
export const Badge: React.FC<BadgeProps>
```

From Site_Limpa_Fossa/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

Design tokens in tailwind.config.js:
```
brand-green: '#16a34a'
brand-blue: '#2563eb'
```

lucide-react is installed -- use for service card icons.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component with sticky nav and mobile hamburger menu</name>
  <files>Site_Limpa_Fossa/src/components/sections/Header.tsx</files>
  <action>
    Create `src/components/sections/Header.tsx` implementing requirement NAV-01 (header with logo and company name visible on all viewports).

    Component specifications:
    - Logo: Text-based "Limpa Fossa Express" using `text-brand-green font-bold text-xl`
    - Nav links: Inicio, Servicos, Agendamento, Contato -- each as `<a href="#section-id">` anchor links
    - Desktop (md+ breakpoint): horizontal nav links inline with logo
    - Mobile (<md): hamburger icon button (use `Menu` and `X` from lucide-react) toggling a dropdown/slide-down menu
    - Sticky behavior: `sticky top-0 z-50 bg-white/95 backdrop-blur-sm shadow-sm` -- always visible on scroll
    - Container: `max-w-7xl mx-auto px-4` for content centering
    - Must NOT cause horizontal overflow at 360px width

    Implementation details:
    - Use `useState` for mobile menu open/close toggle
    - Nav items as array of `{ label: string, href: string }` for DRY iteration:
      ```ts
      const navItems = [
        { label: 'Inicio', href: '#inicio' },
        { label: 'Servicos', href: '#servicos' },
        { label: 'Agendamento', href: '#agendamento' },
        { label: 'Contato', href: '#contato' },
      ]
      ```
    - Mobile menu: render below header as a `<div>` that shows/hides via conditional rendering
    - Close mobile menu when a nav link is clicked (for smooth scroll UX)
    - Use semantic `<header>` and `<nav>` HTML elements
    - Do NOT use React Router -- plain anchor tags only (single-page scroll landing)
  </action>
  <verify>
    <automated>cd Site_Limpa_Fossa && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Header.tsx exports a Header component
    - "Limpa Fossa Express" renders as text logo in brand-green
    - 4 nav links render on desktop, hamburger menu on mobile
    - Header is sticky (stays visible on scroll)
    - No horizontal overflow at 360px
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HeroSection and ServicesSection components</name>
  <files>
    Site_Limpa_Fossa/src/components/sections/HeroSection.tsx,
    Site_Limpa_Fossa/src/components/sections/ServicesSection.tsx
  </files>
  <action>
    **HeroSection** -- implements HERO-01 (headline above fold 360px), HERO-02 (CTA smooth scroll), HERO-03 (LCP < 2.5s):

    Create `src/components/sections/HeroSection.tsx`:
    - Section wrapper: `id="inicio"` for nav link targeting
    - Background: CSS gradient (brand-green to brand-blue diagonal), NOT an image (per PITFALLS.md -- zero image weight for LCP)
      ```
      bg-gradient-to-br from-brand-green to-brand-blue
      ```
    - Text color: white on the gradient background
    - Title: `<h1>` with text "Limpeza de Fossa Rapida e Sem Complicacao" -- use `text-3xl md:text-5xl font-bold`
    - Subtitle: `<p>` with "Atendimento em ate 2 horas. Profissionais certificados." -- use `text-lg md:text-xl opacity-90`
    - CTA button: Use shadcn `<Button>` component wrapped in an `<a href="#agendamento">`:
      - Text: "Agendar Agora"
      - Style: `size="lg"` with white background on colored gradient: `bg-white text-brand-green hover:bg-gray-100 font-bold text-lg px-8 py-6`
      - The `<a>` tag handles smooth scroll via `href="#agendamento"` (CSS `scroll-behavior: smooth` is set in index.css from Plan 01)
    - Secondary CTA: text link "ou fale pelo WhatsApp" with `Phone` icon from lucide-react, pointing to `https://wa.me/55XXXXXXXXXXX` (placeholder number)
    - Badge: Use shadcn `<Badge>` with text "Atendemos em [Cidade] e regiao" -- positioned above the title
    - Layout: centered text, padded (`py-20 md:py-32 px-4`), content within `max-w-4xl mx-auto text-center`
    - CRITICAL for HERO-01: At 360px, the title, subtitle, and "Agendar Agora" button MUST all be visible above the fold (within ~600px viewport height). Keep padding conservative on mobile: `py-12 md:py-20`

    **ServicesSection** -- implements SERV-01 (3 service cards with description and icon):

    Create `src/components/sections/ServicesSection.tsx`:
    - Section wrapper: `id="servicos"` for nav link targeting
    - Section title: `<h2>` "Nossos Servicos" centered, `text-3xl font-bold text-gray-900`
    - Grid layout: `grid grid-cols-1 md:grid-cols-3 gap-6` -- 1 column mobile, 3 columns desktop
    - Container: `max-w-7xl mx-auto px-4 py-16`

    - 3 cards using shadcn `<Card>` component, each with:
      1. **Limpeza de Fossa**
         - Icon: `Droplets` from lucide-react (48px, text-brand-green)
         - Description: "Servico completo de limpeza e esvaziamento de fossas septicas com equipamentos modernos e equipe especializada."
      2. **Desentupimento**
         - Icon: `Wrench` from lucide-react (48px, text-brand-green)
         - Description: "Desentupimento de pias, ralos, vasos sanitarios e tubulacoes com rapidez e eficiencia."
      3. **Caixa de Gordura**
         - Icon: `Container` from lucide-react (48px, text-brand-green)
         - Description: "Limpeza e manutencao preventiva de caixas de gordura residenciais e comerciais."

    - Card structure:
      ```tsx
      <Card className="text-center hover:shadow-lg transition-shadow">
        <CardHeader>
          <div className="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-full bg-brand-green/10">
            <IconComponent className="h-8 w-8 text-brand-green" />
          </div>
          <CardTitle>{serviceName}</CardTitle>
        </CardHeader>
        <CardContent>
          <CardDescription>{description}</CardDescription>
        </CardContent>
      </Card>
      ```

    - Define services as a typed array for clean iteration:
      ```ts
      const services = [
        { icon: Droplets, title: 'Limpeza de Fossa', description: '...' },
        { icon: Wrench, title: 'Desentupimento', description: '...' },
        { icon: Container, title: 'Caixa de Gordura', description: '...' },
      ]
      ```
    - Cards have subtle hover effect: `hover:shadow-lg transition-shadow` (Claude's discretion per CONTEXT.md)

    Note on LCP (HERO-03): No `<img>` tags in either component. Hero uses CSS gradient. Icons are inline SVG via lucide-react (tiny, no network requests). System font stack means no font-loading delay.
  </action>
  <verify>
    <automated>cd Site_Limpa_Fossa && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - HeroSection.tsx exports HeroSection with gradient background, h1 headline, subtitle, "Agendar Agora" button linking to #agendamento, service area Badge
    - ServicesSection.tsx exports ServicesSection with 3 Card components (Limpeza de Fossa, Desentupimento, Caixa de Gordura), each with Lucide icon and description
    - Both components use semantic HTML (section, h1/h2)
    - No images loaded -- CSS gradient and SVG icons only
    - At 360px, hero content fits above the fold
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Footer component and wire all sections into App.tsx</name>
  <files>
    Site_Limpa_Fossa/src/components/sections/Footer.tsx,
    Site_Limpa_Fossa/src/App.tsx
  </files>
  <action>
    **Footer** -- implements NAV-02 (footer with contact info) and CTA-03 (social links):

    Create `src/components/sections/Footer.tsx`:
    - Section wrapper: `id="contato"` for nav link targeting, `bg-gray-900 text-white`
    - Container: `max-w-7xl mx-auto px-4 py-12`
    - Layout: Responsive grid `grid grid-cols-1 md:grid-cols-3 gap-8`

    Column 1 -- Brand:
    - "Limpa Fossa Express" as text logo in `text-brand-green font-bold text-xl`
    - Brief tagline: "Servicos de limpeza de fossa e desentupimento com qualidade e agilidade."

    Column 2 -- Contact (NAV-02):
    - Phone: `<Phone>` icon + "(XX) XXXX-XXXX" (placeholder)
    - WhatsApp: `<MessageCircle>` icon + "(XX) XXXXX-XXXX" -- link to `https://wa.me/55XXXXXXXXXXX`
    - Service area: `<MapPin>` icon + "Atendemos em [Cidade] e regiao"
    - Use lucide-react icons: `Phone`, `MessageCircle`, `MapPin`

    Column 3 -- Social Links (CTA-03):
    - Instagram: `<Instagram>` icon + "Instagram" -- link `href="#"` (placeholder)
    - Facebook: `<Facebook>` icon + "Facebook" -- link `href="#"` (placeholder)
    - Use lucide-react icons: `Instagram`, `Facebook`
    - Links open in new tab: `target="_blank" rel="noopener noreferrer"`

    Bottom bar:
    - Horizontal rule `<hr className="border-gray-700 my-8">`
    - Copyright: "2026 Limpa Fossa Express. Todos os direitos reservados." centered

    **App.tsx** -- wire all sections:

    Replace the test scaffold content in `src/App.tsx` with the final landing page assembly:
    ```tsx
    import { Header } from '@/components/sections/Header'
    import { HeroSection } from '@/components/sections/HeroSection'
    import { ServicesSection } from '@/components/sections/ServicesSection'
    import { Footer } from '@/components/sections/Footer'

    function App() {
      return (
        <div className="min-h-screen bg-white text-gray-900 font-sans">
          <Header />
          <main>
            <HeroSection />
            <ServicesSection />
            {/* Phase 2: AppointmentSection will be added here with id="agendamento" */}
            <section id="agendamento" className="py-16 px-4">
              <div className="max-w-2xl mx-auto text-center">
                <h2 className="text-3xl font-bold text-gray-900 mb-4">Agendamento</h2>
                <p className="text-gray-600">Formulario de agendamento disponivel em breve.</p>
              </div>
            </section>
          </main>
          <Footer />
        </div>
      )
    }

    export default App
    ```

    IMPORTANT: The `#agendamento` placeholder section is REQUIRED so the CTA smooth scroll has a valid target. Without it, "Agendar Agora" would scroll to nothing. This placeholder will be replaced by the real AppointmentSection in Phase 2.

    After wiring, run `npm run build` and `npm run dev` to verify the complete page works.
  </action>
  <verify>
    <automated>cd Site_Limpa_Fossa && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Footer.tsx exports Footer with phone, WhatsApp, service area, Instagram link, Facebook link, copyright
    - App.tsx imports and renders Header, HeroSection, ServicesSection, placeholder #agendamento, Footer in correct order
    - Clicking "Agendar Agora" smooth-scrolls to the #agendamento section
    - Page builds with zero errors
    - At 360px: no horizontal overflow, all sections stack vertically, readable text
    - At desktop (1280px+): 3-column service grid, horizontal nav, multi-column footer
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External links (WhatsApp, social) | Outbound links to third-party services |
| User scroll interaction | Anchor navigation via href targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | Social/WhatsApp links | accept | Placeholder hrefs point to `#` or `wa.me`; real URLs configured in Phase 2+. Low risk -- outbound links only, no user data involved |
| T-01-04 | Information Disclosure | Footer contact info | accept | Phone/WhatsApp are public business contact info intended for display. No PII exposed |
| T-01-05 | Denial of Service | LCP performance | mitigate | CSS gradient hero (no images), system font stack (no font loading), no animation libraries, inline SVG icons via lucide-react -- all per HERO-03 and Pitfall 5 |
</threat_model>

<verification>
1. Run `npm run build` from `Site_Limpa_Fossa/` -- zero errors
2. Run `npm run dev` and open in browser
3. Resize to 360px width:
   - Header shows "Limpa Fossa Express" without horizontal overflow (NAV-01)
   - Hero headline, subtitle, and "Agendar Agora" button visible above fold (HERO-01)
   - Hamburger menu opens/closes nav links
4. Click "Agendar Agora" -- page smooth-scrolls to #agendamento section (HERO-02)
5. Scroll to Services: 3 cards visible with icons (Limpeza de Fossa, Desentupimento, Caixa de Gordura) (SERV-01)
6. Scroll to Footer: phone, WhatsApp, service area, Instagram, Facebook links visible (NAV-02, CTA-03)
7. Run Lighthouse on mobile with simulated 4G: LCP should be < 2.5s (HERO-03) -- no images, no external fonts, no heavy JS
8. Expand to desktop (1280px+): 3-column service grid, horizontal nav, multi-column footer
</verification>

<success_criteria>
- All 7 Phase 1 requirements verified:
  - NAV-01: Header with logo visible at 360px
  - NAV-02: Footer with phone, WhatsApp, service area
  - HERO-01: Headline above fold at 360px
  - HERO-02: CTA smooth scrolls to #agendamento
  - HERO-03: LCP < 2.5s (CSS gradient, system fonts, SVG icons)
  - SERV-01: 3 service cards with icons and descriptions
  - CTA-03: Social links in footer
- Mobile-first responsive design working at 360px and 1280px+
- Zero build errors
- Production-ready visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
